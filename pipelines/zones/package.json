{
  "name": "@spoken/zones-pipeline",
  "private": true,
  "type": "module",
  "description": "Dependencies for the zones pipeline. The scrapers and the addon build stay dependency-free; only the voiceline store needs a Postgres client and only the lore rewrite needs the Claude SDK. A workspace member so that pnpm installs pg here, where voice/db.mjs's upward resolution can find it -- which is what the old repo needed a second, npm-managed root package for.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "pg": "^8.13.1"
  }
}
